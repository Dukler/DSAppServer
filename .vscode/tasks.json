{
    "version": "2.0.0",
    "type": "shell",
    "cwd": "${workspaceFolder}",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "${workspaceFolder}\\build.bat"
        }
    ]
}
